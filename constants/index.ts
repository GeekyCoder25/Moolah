export const ACCESS_TOKEN_KEY = 'accessToken';
export const IS_LOGGED_IN = 'isLoggedIn';
export const LAST_OTP = 'lastOtp';
export const APP_THEME = 'appTheme';
// 30-day server-issued token that lets a "remembered" device skip OTP.
export const TRUST_TOKEN_KEY = 'trustToken';
// Persistent, non-secret device identifier sent with login.
export const DEVICE_ID = 'deviceId';
